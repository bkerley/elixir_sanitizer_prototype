defmodule ElixirSanitizerPrototypeTest do
  use ExUnit.Case
  doctest ElixirSanitizerPrototype

  test "installs and uninstalls"

  test "triggers on injection"

  test "doesn't trigger on non-injection"
end
